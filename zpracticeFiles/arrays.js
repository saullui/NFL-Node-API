function Array(){
    let array1 = [9, 1, 8, 6, 2, 2, 4, 9, 0];
    let array2 = [0, 6, 1, 7, 8, 8, 2, 1, 6];

    console.log("Hello");

    for(let x = 0; x < array1.length; x++){
        if(array1(x) >= 5){
            console.log("This is greater than 4, ");
        }
        if(array1(x) < 5){
            console.log("This is less than 5");
        }
        for(j = 0; j < array2.length; j++){
            if(array1(x) === array2(j)){
                console.log("Array1 and Array 2 has a matching number, ", )
            }
        }
    }
}